# bagging-Churn-Prediction-project-
End-to-end customer churn prediction using Decision Tree, SVM, and Random Forest (bagging/ensemble learning) with full EDA, preprocessing, hyperparameter tuning via GridSearchCV, model comparison, and an interactive prediction form — built in Python/Jupyter.
